# write_articles — pipeline-specific settings.
# Shared keys (API_KEY, MODEL, API_URL, MAX_RETRIES, etc.) come from ../../seo.env.

OUTPUT_DIR=output
INPUT_JSON=input/topics.json
USE_AI=false

DELAY_MS=60000
JITTER_MS=30000
EXTRA_MS_PER_1K_WORDS=5000
MAX_TOKENS=8192
TIMEOUT=60
