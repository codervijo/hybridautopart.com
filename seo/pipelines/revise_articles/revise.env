# revise_articles — pipeline-specific settings.
# Shared keys (API_KEY, MODEL, API_URL, MAX_RETRIES, etc.) come from ../../seo.env.

INPUT_ARTICLES_DIR=input/articles
INPUT_REVIEWS_DIR=input/reviews
OUTPUT_DIR=output

MAX_TOKENS=8192
DELAY_MS=2000
JITTER_MS=1000
TIMEOUT=120
