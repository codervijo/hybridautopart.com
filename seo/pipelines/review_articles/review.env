# review_articles — pipeline-specific settings.
# Shared keys (API_KEY, MODEL, API_URL, MAX_RETRIES, etc.) come from ../../seo.env.

INPUT_DIR=input
OUTPUT_DIR=output

DELAY_MS=20000
JITTER_MS=8000
MAX_TOKENS=4096
TIMEOUT=120
