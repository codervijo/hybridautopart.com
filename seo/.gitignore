# Pipeline-generated artifacts — never commit input staging or output dirs
output/
input/

# Audit pipeline data store — dated snapshots, regenerated by `make audit`
data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Env files with secrets (pipeline-specific .env files are safe to track —
# real credentials live only in seo.env and plugin-builder/builder.env)
seo.env
plugin-builder/builder.env
